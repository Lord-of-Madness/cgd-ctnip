using UnityEngine;

public class ArmatureAnimatorEventHandler : MonoBehaviour
{
	//If this doesn't exist in the object with sample armature animator -> error occurs
	private void OnLand(AnimationEvent animationEvent)
	{
		return;
	}

	//If this doesn't exist in the object with sample armature animator -> error occurs
	private void OnFootstep(AnimationEvent animationEvent)
	{
		return;
	}
}
